// import { useEffect } from "react";
//
// //import useAuth from "../../../hooks/useAuth";
//
export const useAuthorization = (isAuth: boolean) => {
	//const { checkAuth } = useAuth();

	// useEffect(() => {
  //   if (isAuth) return
  //
  //   checkAuth()
  //   // eslint-disable-next-line react-hooks/exhaustive-deps
  // }, [isAuth])

	return {}
}

